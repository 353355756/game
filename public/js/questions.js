$(document).ready(function(){
  CKEDITOR.appendTo('editorSpace');
  /*var doc;
  if(document.all){//IE
    doc = document.frames["ifra"].document;
  }else{//Firefox
    doc = document.getElementById("ifra").contentDocument;
  }
  doc.write( 
    '<!DOCTYPE html>' +
    '<html>' +
    '<head>' +
      '<meta charset="utf-8">' +
      '<script src="../../../js/wiris.js"></script>' +
      '<script type="text/javascript">' +
          'window.onload = function(){' + 
            'alert(1);var editor = com.wiris.jsEditor.JsEditor.newInstance({"language": "en"});'+
            'editor.insertInto(document.getElementById("wiris"));' +
          '};'+
      '</script>' +
    '</head>' +
    '<body>' +
      '<div id="wiris"></div>' +
    '</body>' +
    '</html>' 
  );*/
});